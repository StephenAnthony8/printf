General
	create a buffer of size 1024
Printf handles only case types
	-char
		- '%'as the second input
	- int, octal, binary, hexadecimal, HEXADECIMAL
		- use recursion function to get the values into strings
		--> for the hexadecimals, separate function that converts into hex values
		- unsigned int -> binary
	-pointer
		- p
		- NULL handling
	- float/double
	-string
		- literal string form(5);
		- NULL handling
	-long/short
		--> int, octal, hexadecimal, HEXADECIMAL

	-reversed string & ROT13
	*field width, precision
	* 0, -, +, ' ', #.
	
